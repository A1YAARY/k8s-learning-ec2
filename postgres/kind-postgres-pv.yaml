apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv
  namespace: production
spec:
  capacity:
    storage:  5Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: fast-ssd  
  hostPath:
    path: /mnt/data